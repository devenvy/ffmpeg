# FFmpeg versions maintained by this repo (one exact upstream release per line).
# Each line is built and released independently as tag {version}.{build}
# (e.g. 8.1.2.0, 8.1.2.1). check-updates.yml bumps each line to the newest point
# release in its series, and opens a PR adding a new major series as a parallel
# line when one appears upstream. Add a line (e.g. 9.0) to start maintaining
# another version line in parallel.
8.1.2
9.0.1
